import dashboard from './dashboard';
import employeStagaire from './EmployeStagaire';
import other from './other';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, employeStagaire, other]
};

export default menuItems;
